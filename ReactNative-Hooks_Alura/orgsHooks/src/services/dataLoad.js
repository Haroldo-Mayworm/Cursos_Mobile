import header from '../mocks/header';
import producers from '../mocks/producers';

export const headerLoad = () => {
    return header;
};

export const producersLoad = () => {
    return producers;
};
